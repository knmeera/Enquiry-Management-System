@model Enquiry.Models.Department

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Department</legend>
        <div class="editor-label">
            Dept.Name
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeptName)
            @Html.ValidationMessageFor(model => model.DeptName)
        </div>
                <div class="editor-label">
             Dept.Short Name
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeptShortName)
            @Html.ValidationMessageFor(model => model.DeptShortName)
        </div>

        <div class="editor-label">
            Dept. Description
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeptDesc)
            @Html.ValidationMessageFor(model => model.DeptDesc)
        </div>

         
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
